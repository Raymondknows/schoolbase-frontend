import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { ContentSection, PublicContentShell } from "@/components/public-content-shell";

export const metadata: Metadata = { title: "Use Cases | SchoolBase", description: "See how SchoolBase supports the daily work of schools." };
const cases = [["Fee collection", "/use-cases/fee-collection", "Create fee structures, track invoices and payments, and keep financial records clearer."], ["Parent communication", "/use-cases/parent-communication", "Share school updates and important messages with families through connected channels."], ["Results publishing", "/use-cases/results-publishing", "Move from assessments and teacher entries to reviewed results and parent access."], ["Student attendance", "/use-cases/student-attendance", "Record attendance and give school teams a clearer view of patterns."], ["School broadsheets", "/use-cases/school-broadsheet", "Organise assessment data and produce structured academic reports."], ["Data management", "/use-cases/data-management", "Keep student, staff, academic, and financial records in one school account."]];
export default function UseCasesPage() { return <PublicContentShell eyebrow="Use cases" title="Built around the work schools do every day." description="SchoolBase connects the operational moments that otherwise get split between paper, spreadsheets, messages, and disconnected tools."><ContentSection title="Explore practical workflows"><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{cases.map(([title, href, text]) => <Link key={href} href={href} className="group border border-border bg-white p-6 hover:border-brand/50 hover:shadow-lg"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-brand">SchoolBase workflow</p><h2 className="mt-3 text-xl font-semibold text-foreground group-hover:text-brand">{title}</h2><p className="mt-3 leading-7 text-muted">{text}</p><span className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-brand">Read the use case <ArrowRight className="h-4 w-4 transition group-hover:translate-x-1" /></span></Link>)}</div></ContentSection></PublicContentShell>; }
