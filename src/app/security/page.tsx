import type { Metadata } from "next";
import { ContentItem, ContentSection, PublicContentShell } from "@/components/public-content-shell";

export const metadata: Metadata = { title: "Security & Data Protection | SchoolBase", description: "Understand the practical security controls supporting SchoolBase school operations." };
const controls = [["Account access", "Role-aware workspaces help schools give administrators, teachers, bursars, and parents access to the areas relevant to them."], ["Secure transport", "SchoolBase uses secure connections for information moving between the browser and the service."], ["Operational records", "Activity such as fees, results, attendance, and communication stays organised within the school account that uses it."], ["Support and continuity", "Our team helps schools with setup, access questions, and the practical steps needed to keep daily work moving."]];
export default function SecurityPage() { return <PublicContentShell eyebrow="Security" title="Security that respects the importance of school data." description="SchoolBase is built for records that matter: student information, academic results, attendance, fees, and communication with families. We keep security practical, visible, and part of the product experience."><ContentSection title="The security foundation" intro="Security is supported by product controls, responsible account practices, and clear ownership between SchoolBase and each school."><div className="grid gap-4 md:grid-cols-2">{controls.map(([title, text]) => <ContentItem key={title} title={title} text={text} />)}</div></ContentSection><ContentSection title="Shared responsibility"><div className="grid gap-4 md:grid-cols-3"><ContentItem title="Use strong access" text="Keep passwords private and remove access when a staff member leaves the school." /><ContentItem title="Choose roles carefully" text="Give each person the workspace and permissions needed for their work." /><ContentItem title="Tell us quickly" text="Contact support if you notice unusual activity or need help securing an account." /></div></ContentSection></PublicContentShell>; }
